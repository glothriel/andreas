x-hugo-image: &hugo-image hugomods/hugo:exts-0.139.5

services:
  hugo:
    image: *hugo-image
    volumes:
      - .:/src
    ports:
      - "1313:1313"
    command: server --bind 0.0.0.0 --buildDrafts --buildFuture

